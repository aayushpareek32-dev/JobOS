__pycache__/
*.pyc
*.pyo
.DS_Store
venv/
.env
*.egg-info/
dist/
build/

# LaTeX 中间文件
*.aux
*.log
*.out
*.synctex.gz

# 数据库（用户本地数据，不上传）
data/jobs.db

# 生成的输出文件（作为案例保留部分）
# data/outputs/

# 本地配置（含真实 API Key，不上传）
config.local.yaml

# API Key 不要上传（config.yaml 中有示例，用户自行替换）
